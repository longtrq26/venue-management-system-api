// Metadata key to mark a route as public
export const IS_PUBLIC_KEY = 'common:isPublic';

// Decorator key for roles
export const ROLES_KEY = 'common:roles';

// Token expiry time in milliseconds
export const TOKEN_EXPIRY_MS = 15 * 60 * 1000;
